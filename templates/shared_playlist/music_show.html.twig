{% extends 'base.html.twig' %}

{% block title %}Music{% endblock %}

{% block body %}
    <h1>Music</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ music.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ music.year }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ music.title }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ music.getGenres()|join(', ') }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shared_playlist_show', { 'id': shared_playlist.id}) }}">back to sharde shared_playlists d'un membres</a>

    <a href="{{ path('app_music_edit', {'id': music.id}) }}">edit</a>

    {{ include('music/_delete_form.html.twig') }}

    
{% endblock %}
